{% extends 'base.html.twig' %}

{% block title %}Edit Chambre{% endblock %}

{% block body %}
    <div class="card mb-4">
        <div class="card-header">
            Editer la chambre
        </div>
        <div class="card-body">
            {{ include('chambre/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('chambre/_delete_form.html.twig') }}
            <a href="{{ path('chambre_index') }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
