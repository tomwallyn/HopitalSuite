{% extends 'base.html.twig' %}

{% block title %}Edit Rdv{% endblock %}

{% block body %}
    <div class="card mb-4">
        <div class="card-header">
            Editer le rendez-vous
        </div>
        <div class="card-body">

    {{ include('rdv/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

    <a href="{{ path('listmodif') }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">Retour à la liste</a>
        </div>
    </div>

    {{ include('rdv/_delete_form.html.twig') }}
{% endblock %}
