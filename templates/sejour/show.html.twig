{% extends 'base.html.twig' %}

{% block title %}Sejour{% endblock %}

{% block body %}
    <h1>Sejour</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sejour.id }}</td>
            </tr>
            <tr>
                <th>DateArrive</th>
                <td>{{ sejour.dateArrive ? sejour.dateArrive|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateSortie</th>
                <td>{{ sejour.dateSortie ? sejour.dateSortie|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sejour_index') }}">back to list</a>

    <a href="{{ path('sejour_edit', {'id': sejour.id}) }}">edit</a>

    {{ include('sejour/_delete_form.html.twig') }}
{% endblock %}
