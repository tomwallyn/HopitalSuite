{% extends 'base.html.twig' %}

{% block title %}Edit Lit{% endblock %}

{% block body %}
    <div class="card mb-4">
        <div class="card-header">
            Editer le lit
        </div>
        <div class="card-body">
            {{ include('lit/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('lit/_delete_form.html.twig') }}
            <a href="{{ path('lit_index') }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
